<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Check_for_Location_if_event_is_Virtual</fullName>
    <active>true</active>
    <description>If Virtual is Selected as Value for Event Type field then Prevent User to Select Location on Event Record.</description>
    <errorConditionFormula>IF(ISPICKVAL(Event_Type__c, &apos;Virtual&apos;),  IF(NOT(ISBLANK(Location__c)),True,False)   ,false)</errorConditionFormula>
    <errorDisplayField>Location__c</errorDisplayField>
    <errorMessage>If Virtual is Selected as Value for Event Type field then Location can not be selected</errorMessage>
</ValidationRule>
