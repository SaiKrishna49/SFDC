<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Check_on_Attendee</fullName>
    <active>true</active>
    <description>Attendee can only be associated with the Event whose End Date is in future &amp; Event Live Checkbox is checked and Event is accepting the Attendees ( means Remaining Seats field value is not 0 )</description>
    <errorConditionFormula>IF(NOT( ISBLANK(Event__r.End_Date_Time__c)), 
if(AND( Event__r.End_Date_Time__c   &lt;=   DATETIMEVALUE(TODAY()),
Event__r.Live__c = FALSE, Event__r.Remaining_Seats__c  &lt; 1),true,false),false )</errorConditionFormula>
    <errorMessage>Attendee can only be associated with the Event whose End Date is in future &amp; Event Live Checkbox is checked and Event is accepting the Attendees</errorMessage>
</ValidationRule>
